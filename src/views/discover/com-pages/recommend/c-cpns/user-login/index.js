import React, { memo } from 'react';
import { UserLoginWrapper } from './style';
const UserLogin = memo(() => {
  return <UserLoginWrapper>UserLogin</UserLoginWrapper>;
});

export default UserLogin;
